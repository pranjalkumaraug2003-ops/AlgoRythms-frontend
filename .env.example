# AlgoRythms Frontend Environment Variables

# The base URL for the backend API (e.g., http://localhost:5000 or https://algorythms-api.onrender.com)
VITE_API_BASE_URL=http://localhost:5000

# The base URL for the Party Room service (same backend hosts both REST + WebSocket)
VITE_PARTY_URL=http://localhost:5000
